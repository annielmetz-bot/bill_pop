# Bill Pop — environment configuration template
# Copy to .env and fill in. NEVER commit the real .env (PHI / credentials).

# PostgreSQL connection (SQLAlchemy + psycopg3 driver)
DATABASE_URL=postgresql+psycopg://billpop:billpop@localhost:5432/billpop

# App
APP_ENV=development
